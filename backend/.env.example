# Backend Environment Variables (Example)
# Copy this file to ".env" in the same folder and fill in values.
# Do NOT commit the ".env" file.

# Server
PORT=3001
NODE_ENV=development

# Database
# For local development (host machine)
MONGODB_URI=mongodb://admin:changeme@localhost:27017/asmstudio?authSource=admin
# For Docker network (uncomment if using docker-compose)
# MONGODB_URI=mongodb://admin:changeme@mongodb:27017/asmstudio?authSource=admin

# Frontend (for CORS and share links)
FRONTEND_URL=http://localhost:3000

# Authentication (JWT)
# Generate a strong random secret, e.g.:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change_me_generate_a_random_secret
JWT_EXPIRE=7d

# AI Providers
# Optional: Backend Gemini API key (leave empty if keys are provided per-user on the client)
GEMINI_API_KEY=

# Admin
# Used to protect admin endpoints via the X-Admin-Key header
ADMIN_SECRET_KEY=change_me_admin_secret

# Optional: OpenRouter (used by tools/scripts)
OPENROUTER_API_KEY=
